(0)Create mvn project:
mvn archetype:generate \
-DgroupId=com.example \
-DartifactId=my-project \
-DarchetypeArtifactId=maven-archetype-quickstart \
-DinteractiveMode=false

(1)Compile maven project first time: mvn clean compile

(2)Compile maven project: mvn compile

(3)Package the project: mvn package

(4)Add (a) to the pom.xml
XML:
<project>
# Other code
# Other code
# Other code

  <build>
      <plugins>
          <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-jar-plugin</artifactId>
              <version>3.2.2</version>
              <configuration>
                  <archive>
                      <manifest>
                          <mainClass>ExampleName.com</mainClass> # Enter GrpoupId
                      </manifest>
                  </archive>
              </configuration>
          </plugin>
      </plugins>
  </build>

  # Other code
  # Other code
  # Other code
</project>

(5)RunProgram (With .jar file name that was created within the target folder): java -jar target/NameOfJarFile-1.0-SNAPSHOT.jar

(*)Rebuild Project: mvn clean package


